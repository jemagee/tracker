<% if item.errors.any? %>
	<% item.errors.full_messages.each do |msg| %>
		<%= msg %>
	<% end %>
<% end %>

<%= form_for item do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.submit "#{action} Item" %>
<% end %>