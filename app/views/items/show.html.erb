<h1><%= @item.name %></h1>

<%= link_to "Edit #{@item.name}", edit_item_path(@item) %>
<%= link_to "Delete #{@item.name}", item_path(@item), method: :delete, data: {confirm: "Are you sure you want to delete #{@item.name}"} %>


